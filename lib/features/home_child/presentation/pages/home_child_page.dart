import 'package:flutter/material.dart';

class HomeChildPage extends StatelessWidget {
  const HomeChildPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Center(child: Text('Home Child Page'),),
    );
  }

}